// pages/WorkflowBuilderPage.jsx
// -----------------------------
// Placeholder for the Workflow Builder page.
// Rendered at: /builder (private route — requires authentication)
//
// Day 1: Routing only. No drag-and-drop canvas, no node editor, no UI.
// Future days will add the visual workflow builder.

function WorkflowBuilderPage() {
  return (
    <div>
      <h1>Workflow Builder Page</h1>
    </div>
  );
}

export default WorkflowBuilderPage;
